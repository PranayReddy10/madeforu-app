<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Compose paints the real theme; this only covers the window between
         launch and the first frame, so it matches the app's surface colour
         to avoid a white flash on dark phones. -->
    <style name="Theme.MadeForU" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>
</resources>
