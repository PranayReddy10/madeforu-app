# The live database credentials. NEVER commit this: the repository is
# public. The copy on the server is the authoritative one, and
# sale/config.example.php shows its shape.
sale/config.php

# Receipts and photos uploaded by customers and partners.
sale/uploads/

# Android generated files
android/.gradle/
android/.idea/
android/**/build/
android/local.properties
